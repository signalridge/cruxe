{
  "mcp_servers": {
    "cruxe": {
      "command": "cruxe",
      "args": [
        "serve-mcp",
        "--workspace",
        "${CRUXE_WORKSPACE}"
      ],
      "env": {
        "RUST_LOG": "error",
        "CRUXE_CONFIG": "${CRUXE_CONFIG}"
      },
      "startup_timeout_sec": 30
    }
  }
}
